export enum Tool {
    Unknown = 0,
    Pencil = 1,
    Eraser = 2,
    ColorPicker = 3,
    Camera = 9,
}

export enum ChatMsgVariant {
    SysInfo = 0,
    SysWarning = 1,
    SysError = 2,
    UserMsg = 3,
    UserJoin = 4,
    UserLeave = 5
}